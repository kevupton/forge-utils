/**
 * Constants used throughout the package
 */

/**
 * Default directory for forge-utils cache and configuration files
 */
export const FORGE_UTILS_DIR = '.forge-utils';

/**
 * Default deployments file name
 */
export const DEPLOYMENTS_FILENAME = 'deployments.json';

/**
 * Default meta file name
 */
export const META_FILENAME = 'meta.json';

/**
 * Path to the deployments file relative to the FORGE_UTILS_DIR
 */
export const DEPLOYMENTS_PATH = `${FORGE_UTILS_DIR}/${DEPLOYMENTS_FILENAME}`;

/**
 * Path to the meta file relative to the FORGE_UTILS_DIR
 */
export const META_PATH = `${FORGE_UTILS_DIR}/${META_FILENAME}`;
